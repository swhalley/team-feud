# Overview

Threw a Family Feud Game up to refresh a few React skills. nothing special.

# Setup

`yarn`
`brew add gh`

# Run

`yarn start`

# Deploy

Deployed to GH Pages

Login to GH
`gh auth login`

`yarn deploy`